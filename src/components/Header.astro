---
const navLinks = [
  { href: '#about', label: 'Sobre Mí' },
  { href: '#experience', label: 'Experiencia' },
  { href: '#skills', label: 'Habilidades' },
  { href: '#projects', label: 'Proyectos' },
  { href: '#contact', label: 'Contacto' },
];
---
<header class="fixed top-0 left-0 right-0 z-50 bg-background/80 backdrop-blur-lg">
  <div class="max-w-7xl mx-auto px-6 md:px-10 lg:px-20">
    <div class="flex justify-between items-center h-20">
      <a href="#" class="text-2xl font-bold hover:text-primary transition-colors duration-300">
        EFS<span class="text-primary">.</span>
      </a>
      <nav class="hidden md:flex items-center gap-8">
        {navLinks.map((link, index) => (
          <a href={link.href} class="text-text-secondary hover:text-primary transition-colors duration-300"
             style={`animation: fadeInUp 0.5s ease-out forwards ${index * 0.1 + 0.5}s; opacity: 0;`}>
            {link.label}
          </a>
        ))}
      </nav>
    </div>
  </div>
</header>